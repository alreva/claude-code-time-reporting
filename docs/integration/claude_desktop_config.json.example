{
  "mcpServers": {
    "time-reporting": {
      "command": "/absolute/path/to/time-reporting-system/run-mcp.sh"
    }
  }
}

// Note: Authentication uses Azure Entra ID
// 1. Run 'az login' to authenticate with Azure before using Claude Code
// 2. The MCP Server uses AzureCliCredential to acquire tokens from your Azure CLI session
// 3. Tokens are automatically passed to the GraphQL API and validated
// 4. User identity (oid, email, name) is extracted from validated tokens
