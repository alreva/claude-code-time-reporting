{
  "mcpServers": {
    "time-reporting": {
      "command": "/absolute/path/to/time-reporting-system/run-mcp.sh"
    }
  }
}

// Note: The run-mcp.sh wrapper loads environment variables from env.sh
// Make sure to run './setup.sh' and 'source env.sh' before using Claude Code
// The wrapper validates that Authentication__BearerToken and GRAPHQL_API_URL are set
