query GetAvailableProjects($activeOnly: Boolean!) {
  projects(where: { isActive: { eq: $activeOnly } }) {
    code
    name
    isActive
    availableTasks {
      taskName
      isActive
    }
    tags {
      tagName
      isActive
      allowedValues {
        value
      }
    }
  }
}
