query GetAvailableProjects {
  projects {
    code
    name
    isActive
    availableTasks {
      taskName
      isActive
    }
    tags {
      tagName
      isActive
      allowedValues {
        value
      }
    }
  }
}
