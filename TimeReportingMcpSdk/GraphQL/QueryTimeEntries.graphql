query QueryTimeEntries(
  $where: TimeEntryFilterInput
  $skip: Int
  $take: Int
  $order: [TimeEntrySortInput!]
) {
  timeEntries(where: $where, skip: $skip, take: $take, order: $order) {
    items {
      ...TimeEntryFields
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
