{
  "mcpServers": {
    "time-reporting": {
      "command": "dotnet",
      "args": ["run", "--project", "TimeReportingMcp"],
      "env": {
        "GRAPHQL_API_URL": "http://localhost:5001/graphql"
      }
    }
  }
}